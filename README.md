# data2_watermarking

#dependances à installer
pip install streamlit
pip install numpy 
pip install pillow
pip install opencv-python

# Frontend pour tester:
Lancer la commande et rattacher le dossier à l'app streamlit
streamlit run app.py